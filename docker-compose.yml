services:
  solar-tracker:
    image: ghcr.io/xenofex7/solar-tracker:latest
    build: .
    container_name: solar-tracker
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
